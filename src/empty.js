import { Box } from '@material-ui/core'
import styled from 'styled-components'

const PalletEntryAdd = () => {
    return (
        <StyledContainer>
            abc
        </StyledContainer>
    );
}

const StyledContainer = styled(Box)`
`

export default PalletEntryAdd;